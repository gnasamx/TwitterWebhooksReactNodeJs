import React from 'react';

const Replay = () => {
  return (
    <div>
      <input
        type="text"
        name="input-direct-message"
        id="input-direct-message"
      />
    </div>
  );
};

export default Replay;
